// game.js

// //使飞机随触摸移动
// wx.onTouchMove(function (res) {
//   imgX = res.changedTouches[0].clientX-100 // 将飞机x坐标置为当前触摸点x坐标
//   imgY = res.changedTouches[0].clientY-80 // 将飞机y坐标置为当前触摸点y坐标
// })


import { Main } from './js/main.js'

new Main()